# Assets

Optional files for branding or future use. The header title is **text only** (“The Meadow”) with no icon.

`header-logo.svg` is kept here if you want to add an image beside the title again later (add an `<img>` inside `.header-brand` in `index.html` and style it as needed).
